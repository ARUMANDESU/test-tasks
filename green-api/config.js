// if you what to take it from the environment variables, you can use webpack to bundle it in the build
// but I will keep it simple for now
const config = {
    apiUrl: 'https://7103.api.greenapi.com'
};
